name: Deploy WordPress Theme

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2
        with:
          node-version: '20'

      - name: Deploy Theme to WordPress
        run: |
          scp -r blankslate admin@13.211.5.114:/var/www/html/wp-content/themes
